#Date: 21st August 2026
#Topics Covered:

1) Geeks for geeks, GLASSDOOR - COMPANY REVIEW QUESTIONS
2) CONSTRAINTS - PRIMARY KEY, UNIQUE(NO DUPLICATES, NULL ALLOWED), NOT NULL (NO NULL VALUES), FOREIGN KEY (REFERENCES),
                 COMPOSITE KEY (2 PRIMARY KEY), ALTERNATE KEY(**CANDIDATE KEY =  SUM OF ALTERNATE KEY**), CHECK( TO CHECK THE VALUES),
                 DEFAULT(TO SET DEFAULT VALUES)

3) ADDING CONSTRAINTS IN TABLE, ADDING CONSTRAINT AFTER TABLE CREATION, DROPPING CONSTRAINT.
4) LEARN MORE ABOUT FOREIGN KEY (PARENT AND CHILD TABLES)
     FOREIGN KEY (DEPid) REFERENCES DEPARTMENTS (DEPID) - CASES 
5) COMPOSITE KEY - A COMPOSITE KEY IS A PRIMARY KEY FORMED USING TWO OR MORE COLUMNS. USED WHEN ONE COLUMN ALONE CANNOT UNIQUELY IDENTIFY RECORDS.
     PRIMARY KEY(COL1, COL2) -> CONSIDER THIS AS PRIMARY KEY (BOTH COLUMNS ARE PRIMARY KEY)
6) ALTERNATE KEY - THEORY ONLY (LEARN AND TAKE NOTES )
7) QUESTION - NOT NULL + UNIQUE , PRIMARY KEY (BOTH ARE EQUAL) - FIRST PREFERENCE FOR PRIMARY KEY

